import QtQuick
import AquaMeasure

// Séparateur pour menus déroulants.
Rectangle {
    implicitHeight: 1
    implicitWidth: 10
    color: Theme.border
    topPadding: Theme.s1
    bottomPadding: Theme.s1
}
